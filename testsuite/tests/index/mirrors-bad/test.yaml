driver: python-script
indexes:
    my_index:
        in_fixtures: false
        # Load crates on demand, so each invalid manifest is validated only
        # when its crate is shown (an eager load would abort on the first one)
        lazy_loading: true
