library project Project_Skel is

   For externally_built use "true";
   for Languages use ("C");
   For Library_Name use "project_skel";   -- minus lib*.{a,so}
   For Library_Dir use "/usr/lib";        -- /path/to/lib
   for Source_Dirs use ("/usr/include");  -- where to find relateds *.h
   For Library_Kind use "dynamic";

end Project_Skel;
